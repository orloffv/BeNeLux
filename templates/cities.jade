extends layout

block breadcrumb
    include partials/breadcrumbs
    mixin breadcrumbs(page.metadata.country)

block content
    include partials/cities
    mixin cities(page.metadata.country)
    ul
        each city in cities
            li
                a(href=city.url) #{city.metadata.title}

